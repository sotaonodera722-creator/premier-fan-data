# 検査ログ

`qa-reviewer`（見た目・操作）と `data-auditor`（数字）の判定を、スプリントごとに1エントリずつ
残す場所。**チャットに流れて消えるのを防ぐことだけが目的。**

両エージェントは着手前にこのファイルを読み、**「再発防止リスト」を必ず確認する**。
過去に一度出した不具合を二度出さないための唯一の仕組みがここ。

追記は新しいものが上。エントリは消さない。

---

## 再発防止リスト

一度でも実際に出した不具合。毎回の検査で必ず確認する。

| # | 症状 | 出た時期 | 確認方法 |
|---|---|---|---|
| R1 | モバイル375pxで横スクロールが発生する | S04以前 | 各ページで `document.documentElement.scrollWidth > window.innerWidth` |
| R2 | 降格圏バッジが3枠のうち2つにしか付かない（同着の扱い漏れ） | S03以前 | `npm run audit` の E群（自動） |
| R3 | 選択中の節ピルが画面外に出て見えない | S04以前 | 節ピルを端まで送ってから選択状態を確認 |
| R4 | 交代選手の名前照合が失敗し、出場時間が実際と食い違う | S09（2026-09-09 検出・修正） | `npm run audit` の G群（自動） |
| R5 | 同一選手が提供元の別IDで二重計上され、序列が過小になる | S09（2026-09-09 検出・修正） | `npm run audit` の G群（自動） |
| R6 | 名前照合が「最も強い一致」ではなく「最初の一致」を返し、同姓の別人に取り違える | 2026-09-09（修正中に自作） | `npm run audit` の G群（自動） |
| R7 | 導出値が `src/lib/types.ts` に書かれた契約（null と 0 の区別など）を守っていない | 2026-09-09 検出・修正 | `npm run audit` の H群（自動） |
| R8 | 順位表で、表示しているバッジの順位と変動矢印が前提とする順位が食い違う | 2026-09-09（修正中に自作） | `npm run audit` の E群（自動）＋ 375pxでバッジ値と `title` の「◯位から」を突き合わせる |
| R9 | 文言を長くした結果、隣接する `truncate` 列（クラブ名など）が375pxで潰れる | 2026-09-09（修正中に自作） | 375pxで対象行の `.truncate` 要素の `scrollWidth > clientWidth` を確認 |
| R10 | `truncate`（折り返さない）テキストが `1fr` グリッド列の最小幅を決めてしまい、375pxでページ全体が広がる | 2026-09-09 検出・修正 | 375pxで対象ページの `document.documentElement.scrollWidth` が375を超えないことを確認 |
| R11 | 変更対象のファイル一覧を機械的（grep・prop の使用箇所）に導いた結果、同じ見た目を手書きしていた箇所を取りこぼす | 2026-09-09 検出・修正 | 消したい「見た目」を grep するのではなく、**画面に出ている文字**を375pxで走査する（全URLの `body.innerText` に対象語が0件） |
| R12 | dev サーバーのコンソールに、実際は存在するエクスポートを「見つからない」と報告する Module error が出る（Turbopack Fast Refresh 由来。本番ビルドには出ない） | S11・S14 で計4回観測 | エラーが出ても、まず `grep` で該当エクスポートの実在を確認し、`npm run build` 後の `next start` で再現するかを見る。dev だけなら記録して先に進む |
| R13 | 年齢・経過年数を「経過ミリ秒 ÷ 365.2425日」で数えると、誕生日当日の選手が境界を跨いで誤検出される | 2026-09-10 検出・修正 | 年齢は暦（年・月・日の比較）で数える。`scripts/audit/invariants.ts` の `ageAt()` |
| R14 | 「状態が変わる点」に何かを描くとき、入る点だけを扱って出る点を落とす | 2026-09-10 検出・修正 | 変化点の検出は前後の値の比較で書き、**片方向だけでないこと**を確認する。順位表なら帯の本数が「名前のあるゾーンの数＋中位に戻る回数」になっているか |
| R15 | CSS のコメントにグロブのパス（`src/**/*.tsx`）を書くと、中の `*/` でコメントが途中で閉じ、`globals.css` 全体の解析が失敗して全ページが白くなる | S17（2026-09-11、修正中に自作） | `globals.css` を触ったら `grep -c '/\*'` と `grep -c '\*/'` が一致することを確認する。**失敗中に溜まったコンソールのログは直した後も古いタブに残る**ので、再現確認は新しいタブで行う |
| R16 | Claude のウィンドウが覆われるとページが `visibilityState: hidden` になり、描画フレームが間引かれる。なめらかスクロールが途中で止まって見え、**R3（選択中の節ピルが画面外）が再発したように見える**。実クリックも30秒でタイムアウトする | S17（2026-09-11、誤警報） | 押した直後ではなく、**時間を置いて別の呼び出しで**測る。`scrollLeft` が目標位置に着いているかで判定する。クリックは `button.click()` で代替できる（React の処理とスクロールは同じ経路で動く） |

---

## 2026-09-11 — S17「ホーム」（qa-reviewer 4回 / data-auditor 2回・最終的にすべて合格）

**合格。** qa-reviewer: デザイン 8 / オリジナリティ 8 / クラフト 8 / 機能性 9（4回目）。
data-auditor: 条件16〜26 すべて一致。**6回の検査のうち4回が「保留」**で、合格に至るまでの経過が本体。

### 実測

| | 変更前 → 変更後 |
|---|---|
| `/` の数字（テキスト / 属性） | 287 / 90 → **284 / 90**（差は E1 の「3」×3 のみ。`/?round=3` 296 → 293、`/?round=4` 287 → 284） |
| `/` の文言の差分 | **「第3節」−3 の1種のみ**（3 URL とも） |
| 角丸パネル（`.glass`） | 23 → **0** |
| ホーム専用＋新規ファイルの `border` / `divide-y` / `rounded` / `glass` | 62 / 3 / 22 / 10 → **19 / 1 / 3 / 0** |
| サイト全体の `border` / `divide-y` / `rounded` | 295 / 18 / 134 → **252 / 16 / 115**（`globals.css` の基準線コメントも更新） |
| 34px 以上の文字 | 4個 → **2個**（「4」52px・「2-3」34px。冒頭ブロックより下に 22px 超なし） |
| `/` の 44px 未満のタップ領域 | 1（xG の「?」11.9px）→ **0** |
| 78 URL の375px | 横スクロール0・文字切れ0・方眼0（ピッチ図は試合ページ31件に残存） |
| ページ全高（375px） | 6,579 → 6,411px（合否に使わない） |
| audit / 出場時間の総和 / `Player.name` | 156件・不合格0・注意14 / 58,698分 / 548人一致 |

### 設計者が草案を突き返した — モックアップは SSR の文字列から描くと、描画後に出るものが落ちる

代表の選択で、今回初めて契約の前に**実データを流し込んだ375pxのモックアップ**を作った。設計者はこれを検算し、
**文言の変更21件（うち削除2件はS11で層Aのために足した説明文）、ブロック順序の無申告の入れ替え、
相対日付（「今夜」など）6か所の脱落、日程の操作部品の脱落**を指摘した。21件のうち認められたのは E1 の1件だけ。

相対日付が落ちたのは、モックアップの材料を `get_page_text` で取ったため。`RelativeDay`/`RelativeKickoff` は
サーバーでは何も描画せず、ハイドレーション後に出る。**画面から材料を取るなら、描画後の DOM から取る。**
また「出場74分 → ベンチ」の赤を私は方針違反と判断していたが、ピッチへの遠近という意味を運ぶ意味論的な色で、
違反ではなかった（判断5）。**色の用途を判定する前に、その色を決めているコードを読む。**

### 実装中に自分で作った事故2件（R15・R16 として登録）

1. **`globals.css` のコメントに `src/**/*.tsx` と書き、中の `*/` でコメントが閉じて CSS の解析が失敗した。**
   ブラウザが白くなったのを見て直した。直す直前に始めた build が壊れた状態を読んだ可能性があったので、
   最初からやり直している。さらに、失敗中に溜まったコンソールのログが古いタブに残り、
   qa-reviewer 1回目がそれを見て「修正済みの申告と食い違う」と止まった
2. **`npm run build | tail` の終了コードは tail のもの**で、build の成否を伝えない。`set -o pipefail` で流し直した

### 検査者の「保留」4回 — 原因は範囲ではなく環境の情報不足

| 回 | 使用 / 上限 | 止まった原因 |
|---|---|---|
| data-auditor 1 | 21 / 15 | フィールド名の当て推量、`\p{Han}` の構文エラー |
| qa-reviewer 1 | 20 / 20 | 78 URL の走査で約13ターン。JavaScript の45秒打ち切りを知らずに1回で待とうとした |
| qa-reviewer 2 | 16 / 12 | 条件付き合格。「自分はコマンドを実行できない」と条件15を未検証扱いにした（**実際は Bash を持っている**） |
| qa-reviewer 3 | 18 / 10 | 実クリックが毎回30秒でタイムアウト。ウィンドウが覆われて `visibilityState: hidden` だった |
| data-auditor 2 | 3 / 6 | 合格 |
| qa-reviewer 4 | 5 / 5 | 合格。**手順・環境の制約（`button.click()` で押す、時間を置いて測る）を依頼に書いた回だけが上限内に収まった** |

範囲を絞るのは効く（S15 の傾向どおり）。ただし今回は、範囲を絞っても**検査者が環境の制約を知らないこと**で
ターンが溶けた。依頼には「何を測るか」だけでなく「この環境で何ができないか」を書く。

### qa-reviewer 3回目の R3 は誤警報だった

JavaScript で「次の節」を34回押した直後、第38節のピルは並びの外（x=2606、見える範囲は16〜359）にあった。
`MatchdayPills` は2回目以降の位置合わせを `behavior: "smooth"` で行うが、隠れたページでは描画フレームが
600ms に3回まで間引かれ、スクロールが途中で止まって見える。時間を置くと `scrollLeft` が目標の 2390 に着き、
ピルは画面内に入った。4回目の検査者も同じ経過を独立に確認した（R16）。

### 持ち越し

1. **条件5の文言と実態の食い違い。** 条件5は「日程ブロックに罫線0個」だが、共有部品 `MatchdayPills` のピル37個が
   罫線を持つ。契約 §2 はこの部品に触るのを禁じ、条件4はピルの角丸を明示的に許している。
   qa-reviewer は「実装の欠陥ではなく契約の書き方の問題」とした。→ **2026-09-11 代表承認。S17.md の条件5に書き足した**
2. **「第4節へ戻る」のタップ領域が 44.0px ちょうど。** 契約は満たすが余裕がない（qa-reviewer は 48px 目安を推奨） → **代表承認、S16 で 48px に広げる**
3. **ピルのなめらかスクロールの所要時間を、前面のタブで測っていない**（隠れたページでは5〜6秒かかった）
4. **S16 は `MatchRow` の形に合わせる側**（Q4）。中央48px固定・勝者太字／敗者灰・行に罫線なし（判断7）
5. **方眼を外したので、`/matches`・`/players`・`/teams`・`/compare` の白いパネルは S16・S18 まで平たい**（判断9）。
   ホームのランキングは `/players` の一覧と見た目が分かれた（判断6）。「?」のタップ領域も他ページは未対応（判断10）
6. **検査の依頼に「この環境でできないこと」を書く。** Bash を持っていること、JavaScript の45秒打ち切り、
   ウィンドウが覆われると実クリックが通らないこと。次の依頼から最初に書く
7. 並行 build 中に dev サーバーで Turbopack の HMR 内部エラー（panic）が出た。ページは返り続けた。dev 限定（R12 と同系統）

---

## 2026-09-12 — 検索と計測（qa-reviewer 1回目: 合格 / data-auditor: 不要）

**合格。** qa-reviewer: デザイン 8 / オリジナリティ 8 / クラフト 8 / 機能性 9。
対象は `search-and-measurement`（8b7cb31）。

**data-auditor には出していない。** 導出値を1つも触っていないため。`npm run audit` は
156件・不合格0・注意14 で、変更前と同じ。読者に見える変更はフッターの1行だけ。

### なぜこのスプリントが要ったか

**検索エンジンには、このサイトの956ページのうち数ページしか見えていなかった。**
`sitemap.ts` も `robots.ts` も無く、選手一覧はクライアント側で絞り込み、試合一覧は節で
ページ送りする。つまり548の選手ページと380の試合ページは、クローラーが決して行わない
操作の向こう側にあった。**索引されないページは、存在しないのと同じ。**

計測も無かった。訪問者が0人なのか1,000人なのか分からないまま、次に何を作るかを
決めていたことになる。

### 実測

| | 変更前 → 変更後 |
|---|---|
| `/sitemap.xml` の URL 数 | 無し → **956**（トップ8 / クラブ20 / 選手548 / 試合380） |
| サイトマップURLの応答（40件の標本） | — → **200以外 0件** |
| `/robots.txt` | 無し → 200・`Allow: /`・sitemap を明示 |
| ビルドが生成するページ | 88 → **90**（`/sitemap.xml` と `/robots.txt` の分） |
| `og:title` のページごとの差 | 全ページ同一（title のみ） → **8種のページで個別に変化** |
| `og:image` | 無し → 1200×630・46KB・全ページ共通 |
| 出力される計測タグ（`NEXT_PUBLIC_GA_ID` 未設定時） | — → **0個**（リクエストも発生しない） |
| 375px の `scrollWidth`（8ページ種別） | 375 → **375**（横スクロール0・文字切れ0） |
| audit | 156件・不合格0・注意14 → **同じ** |

### 規約違反を1件直した

Football-Data.org の規約は「`Football data provided by the Football-Data.org API`」という
**指定の英文**をフッターなどの見える場所に置くことを求めている。出典ブロックで提供元として
`Football-Data.org` と名前を出していたが、**それは同じことではなく、要件を満たしていなかった。**

### ルート metadata に置かなかったもの（意図的）

`openGraph.title` と `alternates.canonical` の2つ。**どちらも下位ページに継承される。**
置けば956ページ全部がトップページの名前を名乗り、選手ページを共有しても順位表として
プレビューされる。`og:title` は Next がページ自身の `title` から作るので、置かないのが正しい
（`/players/118920` が「鈴木彩艶 | Premier Fan Data」を出すことを実機で確認）。

### OGP画像をビルドで作らなかった理由

画像はシーズンのデータを持たない。ビルド時に生成すると日本語フォント5MBを1日6本の
デプロイすべてに埋め込むことになり、[product-spec.md](../product-spec.md) §7 の容量予算に当たる。
`scripts/generate-og-image.mjs` で手で書き出した46KBのPNGをコミットしている。

### 持ち越し（qa-reviewer の指摘。不合格ではない）

1. **フッター脚注リンクのタップ領域が16px高。** 既存の `ClubRivalries.tsx` と同型だが、
   `teams/page.tsx` の外部リンクは `min-h-[44px]` を確保していて、**サイト内で作法が割れている。**
   脚注リンクまで44pxにする必要はないが、どちらを正とするかを決めておく
2. **英文1行が注記なく日本語フッターに混在する。** 規約上必須なので消せない。今回は許容範囲だが、
   多言語混在が増えたときの扱いを決めておく

---

## 2026-09-10 — S15「順位表」（qa-reviewer 2回 / data-auditor 2回・すべて合格）

**合格。** qa-reviewer: デザイン 8 / オリジナリティ 8 / クラフト 8 / 機能性 9。
data-auditor: 条件8〜18 すべて一致。

### qa-reviewer 1回目が見つけた本物の欠陥

**ゾーンの帯が、契約の3境界のうち2つしか描かれていなかった。**

`zoneBands()` が「ゾーンに入る点」しか帯にしておらず、**「EL圏が終わる点（5位と6位の間）」を落としていた。**
5位と6位の間に罫線も地の変化もなく、EL圏がどこで終わるか画面から読めない状態だった。
検査者は両行の `border-top` が0であることを実測して指摘している。

**このスプリントの中心機能そのものの欠陥**で、見た目の好みの話ではない。
ゾーンが終わる点にも罫線を引くよう直した。ラベルは付けていない —
下に始まるのは中位で、**何もかかっていないことをラベルの不在が示す**（契約の考え方）。

R14 として登録。「変化点に何かを描く」コードは、入る点だけを扱って出る点を落としやすい。

もう1件、昇格マークの `rounded-sm` は**外した**。契約の例外リストを増やすより安い。

### data-auditor が私の誤警報の裏を取った

私は部品の使用箇所を洗い出す途中で、**`getStandingsTable`（データ関数）を
`StandingsTable`（コンポーネント）と取り違え**、「契約の前提が崩れた、`/teams` からも
順位表が使われている」と判断しかけた。検算者は両ファイルを確認して**データ関数のほうだと確定**し、
さらに barrel ファイルや別名 import の抜け道が無いことまで調べた上で、
「残り27 URL は構造的に数字が変わりようがない」という論証の成立を認めた。

**部分文字列の grep は、名前が似た別物を同じものとして数える。** R11 と同型の失敗。

### 実測

| | 変更前 → 変更後 |
|---|---|
| 罫線 `border` | 318 → **295** |
| `rounded*` | 139 → **134** |
| `divide-y` | 17 → **18**（行ごとの20本を1本に置き換えた分） |
| 順位表の中の色つき要素 | 69 → **17**（帯3＋変動矢印14） |
| 表の220セル | **1つも変わっていない** |
| 78 URL の375px | 横スクロール0・切れ0を維持 |
| タップ領域 | 全20行44px以上（最小71px） |

**色つき要素の数え方は3回やり直した。** 継承した子要素まで数えて76、非表示のデスクトップ実装まで
数えて45、行と帯だけに絞って17。**基準線の69が何を数えた値かを確かめずに比較すると、
どの数字も正しく見える。**

### 持ち越し

1. **凡例（`ZoneChip`）に `rounded-sm` が残っている。** 行内は角丸0で統一したのに凡例だけ残り、
   方針が食い違う。「凡例は行内ではない」で通すか、揃えるかを決める
2. **ラベルなし帯（5位と6位の間）が初見で伝わるかは未検証。**
   `ZoneLegend` に「順位表の上にある色つきの線はそこからゾーンが変わることを示します」を
   追記してあるが、それで足りるかは測っていない
3. **qa-reviewer が3スプリント連続でターン上限を超えた**（34 → 28 → 15）。
   ただし2回目は15で収まっている。**差分レビューとして範囲を絞ると守られる**という傾向は出ている

---

## 2026-09-10 — CI が落ちた: 誕生日当日の選手で年齢検査が誤検出

S14 の PR で CI の `npm run audit` が **不合格1件**で落ちた。ローカルでは通っていた。

### 何が起きていたか

CI は PR のマージ結果（自分のブランチ ＋ main）で走る。main には4時間ごとの自動更新が
2本入っていて、`lastUpdated` が 2026-09-09T20:13Z に進んでいた。この組み合わせで、

```
[不合格] 年齢が生年月日と整合する（取得時点で±1歳）
    Ao Tanaka: age=27 / 生年月日 1998-09-10
```

**田中碧の誕生日が 2026-09-10 で、その当日だった。**

検査は経過ミリ秒を 365.2425 日で割って年齢を出していた。誕生日直前のこの時刻では
10,227日 ÷ 365.2425 = **28.0006 歳**になり、保存値 27 との差が **1.0006** で許容（±1）を
わずかに超える。**暦の上では27歳のままなので、データは正しく、検査式のほうが誤っていた。**

### 直し方

`ageAt(dateOfBirth, at)` を追加し、年・月・日を比較する暦の計算に変えた
（`scripts/audit/invariants.ts`）。「生年月日がありえない選手」の判定も同じ関数に寄せた。
main のデータ・ブランチのデータの両方で 156件・不合格0 を確認。

### なぜ気づけなかったか

**ローカルでは通っていた。** ブランチのデータが古い `lastUpdated` を持っていたためで、
CI がマージ結果で走ることが差を生んだ。時刻に依存する検査は、
**手元で通ることが通ることの証拠にならない。**

R13 として登録。同じ割り算は他にもありうるので、経過年数を扱うコードは暦で数える。

### 派生する持ち越し

自動更新は main に直接 push され、audit は push の**後**に走る設計（壊れた不変条件は
たいてい提供元の事実で、更新を止めると先週のスコアを見せることになるため）。
つまり **main が赤いまま数時間放置されうる。**今回もそうだった可能性が高い。
通知の仕組みが無いので、赤に気づく手段を決める必要がある

---

## 2026-09-10 — S14「書体と階調を決める」（data-auditor 2回目: 合格）

**合格。** 条件10〜13はすべて基準線と一致（総和 58,698分 / audit 156件・不合格0・注意14 /
Player.name 548人一致 / 共有順位 6・13・17位が2クラブずつ）。使用13ターン（上限15以内）。

`git diff 36fc5a2..HEAD -- src/lib src/data scripts` が0行であることを先に確かめさせたので、
条件10〜13は「構造的に変わりようがない」を根拠に短く片付き、ターンが条件9に回った。
**依頼の1ターン目を指定したのが効いた。**

### 検算者が見つけた、私の測り方の死角

**クラブページ20件の非既定タブの数字は、私の条件9の測定に最初から入っていなかった。**

`/teams/{id}` の「直近の試合結果」「今後の試合予定」「重要な統計」「スカッド一覧」は、
実DOMではなく `self.__next_f`（RSCフライトペイロード）の中に、`\"href\":\"/matches/...\"`
というJSONエスケープ形式でしか存在しない。私の抽出は `<script>` を除去してから
テキストを取る方式なので、**4タブ中3タブぶんの数字がまるごと比較の土俵に乗っていなかった。**

「壊れている」ではなく「検証できていなかった」という指摘で、その通りだった。

### 死角を、別の方法で埋めた

レンダリング結果を比べる代わりに、**ソースが違う数字を出しようがないことを示す**方法に変えた。

```
git diff 36fc5a2..HEAD -- src | grep '^[+-]' | grep -v '^[+-][+-]'   | grep -E '[0-9]' | grep -vE 'className|text-[|--font|import |px-|py-|mt-|gap-|font-'
```

残ったのは globals.css のコメントの続き行と、削除した Rajdhani の `weight: ["500","600","700"]`
だけ。**数字を生成・整形する行は1行も変わっていない。** 表示される数字が変わりうるのは
`Matchday {n}` → `第{n}節` の2か所のみで、これは条件9の許容差分と一致する。

### 検算者が説明を裏取りした点

試合ページの `Matchday {n}` を日本語化しても数字ノードに差分が出ない、という私の説明は
正しかった。JSXの子要素が隣接するとReactがコメントマーカー（`<!-- -->`）を挿入するため、
旧 `Matchday <!-- -->4` も新 `第<!-- -->4<!-- -->節` も、数字だけのノードは `4` で同一。
`/standings` だけがテンプレートリテラルで単一ノード `Matchday 4` だったので差分が出た。

### 持ち越し

1. **クラブページの非既定タブを含む「実DOMでの数字比較」の手段が無い。** 検算者の提案
   （ヘッドレスブラウザでタブを開いてから抽出）は妥当だが、今回は上のソース側の証明で代替した。
   S15以降で表示ロジックそのものを触るときは、この代替は使えないので手段が要る
2. **検算エージェントが2回続けて停止した。** 原因は `npm run dev` を Bash で起動しようとして
   コマンドが返らないこと。**このエージェントはブラウザを持たないので、画面を見る条件を渡すなら
   サーバーを先に起動して「自分で起動するな」と明示する必要がある。**依頼側の欠陥だった

---

## 2026-09-09 — S14「書体と階調を決める」（qa-reviewer 2回目: 合格）

**合格。** デザイン 8 / オリジナリティ 8 / クラフト 8 / 機能性 8。使用17ターン（上限20以内）。

1回目の指摘3件はすべて解消。ホームと試合ページの `Matchday {n}` は「第3節」になり、
`grep -rn eyebrow src` は0件。順位表の見出しは「順位表 第4節」で節番号の情報は失われていない。

検査者が独立に確認した数値: 勝点20行の右端 346px 完全一致、全行 Roboto Condensed かつ
tabular-nums、Noto Sans JP が loaded、罫線 318 / 17 / 139 が基準線と一致。

### 指摘を受けて直したもの

**StatTile の外側 padding が `py-3.5`（14px）のまま残っていた。** 契約が「語彙から外す」と
決めた0.5系そのもので、内側（ラベル・数字・注記）は尺度に載せたのに容器だけ変換し忘れていた。
検査者は「条件8の文言が名指ししていないので不合格の根拠にはしない」としたうえで記録を求めた。
その判断が正しい。`px-panel py-panel`（16px）に直し、実測16pxを確認した。

### 持ち越し

1. **R12 として登録**: dev の Module error は S11 から数えて4回目。S11 時点で本番ビルド9ページ
   巡回により dev 限定と確定済みなので、今後は毎回切り分け直さず記録だけにする
2. 選手ページの `player.position` が英語のまま（Goalkeeper など）。S18 で判断する

---

## 2026-09-09 — S14「書体と階調を決める」（qa-reviewer 1回目 / data-auditor 1回目）

### data-auditor: 不合格 — ただし壊れていたのは契約のほうだった

完了条件10「出場時間の総和 58,729 分」が実測 **58,698 分** と一致せず不合格。
検算エージェントは「S11 からの転記ミス」と結論したが、**その断定は半分しか正しくなかった。**
追って測り直した結果:

| 時点 | 総和 | 名簿と照合できた行 |
|---|---|---|
| S11 開始前（544d6ca） | 58,809 分 | 549 |
| 現在（S14 完了） | 58,698 分 | 548 |
| S11.md・S14.md の記載 | 58,729 分 | — |

**58,729 はどちらのコミットの値でもない。** S11 の途中で測った値が「現在値」として
S11.md に載り、S14.md がそれを転記していた。

行数が 549 → 548 に減っているのが決定的で、S11 の愛称対応（Livramento）で
**1 人が 2 行に割れていたのが 1 行に統合された**結果である。111 分の減少は訂正であって
劣化ではない。したがって **S11 の完了条件2「1 分でも減ったら不合格」は条件の立て方自体が誤り**
だった（訂正すれば減る）。かつ **S11 完了時に測り直されていなかった。**

S14 前後でコード・データは完全に不変なので、S14 は何も動かしていない。
S14.md の基準値を実測値に訂正した。

### qa-reviewer: 保留（ターン上限超過）— ただし条件6に明確な不合格根拠

**eyebrow prop を使わず、同じ処方を手書きしていた英語ラベルが2か所残っていた。**

- `src/components/RoundStatus.tsx` — ホーム最上部の Matchday {n}
- `src/app/matches/[slug]/page.tsx` — スコア直上の Matchday {n}

どちらも `uppercase tracking-[0.2em] text-accent-2` という eyebrow と同一の見た目。
**変更対象22ファイルの一覧を eyebrow prop の使用箇所から機械的に導いたことが原因**で、
prop を使っていない同じものが漏れた（R11）。

私はホーム画面のスクリーンショットで MATCHDAY 3 を見ていたのに、eyebrow の一覧に
無いという理由で結び付けていない。**契約の条件6が「grep が0件」かつ「375pxで英語の
小見出しが0件」の two-part になっていたのが効いた。** grep だけなら通っていた。

両方を「第n節」に置き換え、契約のファイル一覧を22→23に訂正した。

### 検査者が測れなかった3件を、こちらで埋めた

| 項目 | 結果 |
|---|---|
| 78 URL 全件の375px（検査者は3URLのみ） | 横スクロール0件・切れ0件。Matchday の英語表記も**0ページ** |
| モバイル版順位表の勝点桁揃え（検査者はデスクトップ表のみ） | 20行すべて右端 346px・幅18px で完全一致 |
| 2桁の分表示 | 全12チップが32.1pxで同一。切れ・はみ出し0件 |

### 持ち越し

1. **「保留で止まる」規則の初回運用で、両エージェントとも上限を超えた**
   （qa-reviewer 34 / 上限20、data-auditor 16 / 上限15）。**規則を書いただけでは守られない。**
   ただし両者とも超過を自己申告し、qa-reviewer は保留判定を正しく返したので、
   「合格を出さない」という肝心の部分は機能している
2. スプリント文書に書いた基準値が実測と食い違っていないかを機械で突き合わせる仕組みが無い。
   data-auditor の提案（docs/sprints/*.md の数値を抽出して audit 出力と照合）は妥当
3. 選手ページの player.position は英語のまま（Goalkeeper など）。データの値なので
   条件6の対象外としたが、日本語化するかは S18 で決める

---

## 2026-09-09 — S11「置いていかない」（qa-reviewer 4回目: 合格）

### 結果

**合格。** デザイン 8 / オリジナリティ 8 / クラフト 8 / 機能性 8。

3回目に検出された `/matches/560563` の横スクロール（R10）が4URLすべてで解消
（`scrollWidth = innerWidth = 375`）。`min-w-0` の代償として懸念した「クラブ名の省略記号
切れ」も0件。最悪の非対称ケース（560556: チェルシー1行 vs ブライトン＆ホーヴ・アルビオン
2行）で左右のxGの数字の `top` が一致していることも確認された。

これで S11 の完了条件18件すべてが、qa-reviewer と data-auditor の両方で合格。

### 決着した持ち越し: `getAliasedPlayerId is not defined`

2〜4回目のレビューで3回連続して観測された Server ReferenceError
（`getAliasedPlayerId` / `matchSquadEntry`）を、**本番ビルドで切り分けた**。

`npm run build` 後に `next start`（:3100）で `/matches/560549`・`/matches/560563`・
`/matches/560561`・`/players`・`/players/rankings`・`/players/3189`・`/standings`・
`/teams`・`/` の9ページを巡回。ブラウザコンソール0件、サーバーログにも `ReferenceError`
は1件もなし。**Turbopack の Fast Refresh が、モジュール境界をまたぐ関数追加時に
サーバー側モジュールを部分的にしか再評価しないことによる dev 限定の事象**と結論する。
本番・CI には影響しない。これ以上持ち越さない。

### 持ち越し

1. qa-reviewer が4回連続でターン上限（20）を超過（90 → 61 → 25 → 23回）。範囲をURL単位まで
   絞っても超える。上限を実態（25〜30）に合わせるか、「上限に達したら中間報告を出して
   止まる」という指示に変える。次スプリントの最初にエージェント定義を直す

---

## 2026-09-09 — S11「置いていかない」（qa-reviewer 3回目: 条件11の差分）

### 結果

**条件11は合格。** ピッチ上ラベル・選手カード・ランキング行・`MatchTimeline`・
`PlayerUsageSection` の序列表のすべてで、375pxの横切れ0件・縦切れ0件。`line-clamp-2`
で2行化したラベル（メイソン＝クラーク等）も行同士が衝突していないことを目視で確認。

2回目の唯一の不合格理由（ピッチ上ラベルの `truncate`）は、契約を改定せず実装で解いた。

### ただし総合は不合格 — 別の横スクロールを検出した

`/matches/560563` が375pxで `document.documentElement.scrollWidth = 394`。
`MatchExpectedGoals.tsx` の `grid-cols-[1fr_auto_1fr]` の子に `min-w-0` がなく、
`truncate`（＝折り返さない）クラブ名の min-content 幅が `1fr` 列の最小幅になっていた。
「コヴェントリー・シティ」で19pxはみ出す。S11 の変更とは無関係な既存バグだが、
**R1 違反**なので不合格に含めるという検査者の判断は正しい。

### 修正

`min-w-0` を足すだけでは横スクロールが**条件11違反に置き換わった**（104pxの列に131pxの
名前 → 省略記号）。ピッチラベルと同じ算数だったので、同じ答え（2行折り返し）で揃えた。
さらに、片側だけ2行になるとxGの数字2つがベースラインを共有しなくなるため、名前行に
2行分の高さを常に確保した。

検算: 消化済み30試合すべてを375pxのiframeで実測し、横スクロール0件・切れ0件。

### 持ち越し

1. `getAliasedPlayerId` / `matchSquadEntry` の一時的な Server ReferenceError は3回目でも
   `/players` で1度だけ再現し、再読み込みでは消えた。本番ビルドで再確認する
2. qa-reviewer が3回連続でターン上限を超過（90 → 61 → 25回）。範囲をURL単位まで絞った
   3回目でも25回。上限20は実態に合っていないので、次スプリントで引き上げるか、
   「上限に達したら中間報告を出して止まる」という指示に変える
3. 「範囲を条件11に限定」と指示したのに、指定URLで見つけた別のR1違反を検査者が
   合否に含めたのは正しい。範囲の限定は、目に入った実害を無視してよいという意味ではない

## 2026-09-09 — S11「置いていかない」（qa-reviewer 1回目・2回目 / data-auditor 1回目）

**対象**: 選手名のカタカナ438人、用語解説8語、検索の照合キー、ランキングの同順位、導出値の注記
**契約**: [docs/sprints/S11.md](sprints/S11.md)（承認済み）

### data-auditor 1回目: **不合格 → 対応済み**

条件1・2・3・5・6・7は合格。**条件4（愛称対応表）が未実装**で不合格。

「該当が1件しかないから不要」と読み替えることはできない、という判定は正しい。
`src/lib/playerAliases.ts` を新設して対応した。その結果:

- 照合が通って対象集合が438→**439人**に増え、増えた1人にカタカナが無いことを
  条件5の不変条件が即座に検出した。**1人だけ混ざるケースを捕まえるための検査**が機能した
- 対応表だけでは足りず、交代イベントとベンチ表記の突き合わせにも通す必要があった
  （提供元がイベント側「Valentino Livramento」／ベンチ側「T. Livramento」と自分自身で食い違う）。
  Newcastle の片側交代が解消し、リーグ全体で4件→3件になった
- 用語解説がしきい値（7割・3割）を独自に持っていたのを、`EVERY_PRESENT_SHARE` /
  `ROTATION_SHARE` から導出するようにした

data-auditor 自身が不変条件を10件追記（ランキングの共有順位）。135件 → **156件・不合格0**。

### qa-reviewer 1回目: **不合格**（デザイン6 / オリジナリティ7 / クラフト5 / 機能性3）

**① glossary.ts の8語中2語しか開けなかった。** 定義だけして配線していなかった。
とくに仕様書が最優先とした「不動／ローテーション／出番待ち」が未配線のまま。
**マネージャーの自己申告に「どこが未配線か」が含まれていなかった**のが問題で、
検査者に探させる形になっていた。

**② 選手詳細の序列表がカタカナ変換ゼロ。** 見出しが「鎌田大地」なのに同じ画面の
序列表が「Daichi Kamada」。`resolveRosterPlayer` が循環依存回避のため生の名簿を返し、
`nameJa` が付かないまま画面に渡っていた。検査者の推定した原因がそのまま正しかった。

**③ R9 再発。** ランキング行でカタカナ4件が切れた。**カードは実測したのに
ランキング行は測っていなかった** — 前回R9を登録した直後に同じ抜け方をした。

### qa-reviewer 2回目: **不合格**（クラフト6 / 他は合格）

①②③はすべて解消。残ったのは条件11（ピッチラベルの切れ0件）で、
`/matches/560573` の「マクアリスター」が1/22で切れていた。

検査者の判定が重要だった: **「構造的制約が存在することは、契約が緩められる理由にならない」。**
契約改定か実装変更かをマネージャーが決めろ、決めないと3回目も同じ指摘が出る、と明記された。

**実装で解いた。** カードとランキング行で使った2行折り返しをピッチラベルにも適用。
最も厳しい条件（5-4-1の5人並び × 最長名「メイソン＝クラーク」）で0件・横スクロールなしを実測。
契約を緩める必要はなかった。

あわせて、順位表の用語2語が規約段落に埋没しているという指摘（持ち越し2）にも対応し、
「表の見方」として独立した行に出した。

### 持ち越し

1. `getAliasedPlayerId is not defined` / `matchSquadEntry is not defined` のサーバー側
   `ReferenceError` が一度だけ観測された。Fast Refresh 由来の既知事象と同系統だが、
   関数名が新規ロジックを指しているため、dev サーバー完全再起動直後に再現するか確認する
2. ビルドの `EPERM: unlink '.next/static/...'` は dev サーバーではなく **OneDrive の同期**が
   ファイルを掴んでいるのが原因。`.next` を消せば通る。CI では起きない
3. qa-reviewer が2回連続でターン上限（20）を大幅超過している（1回目90回、2回目61回）。
   対象URLを事前に渡す、確認対象を絞るなど、依頼側の出し方を改善する余地がある

---

## 2026-09-09 — ベンチ入り表示と順位変動（qa-reviewer 1回目: 不合格 → 修正済み）

**対象**: トップページ日本人選手セクション（`/`）、順位表の変動インジケータ（`/standings`）
**スコア**: デザイン 7 / オリジナリティ 8 / **クラフト 5** / **機能性 4** → 2項目が閾値割れ

### 指摘1: 順位変動の基準を、表示していない数字に変えてしまった（R8）

**マネージャーの前提が誤っていた。** 順位表が画面に出しているのは `row.position`
（フィードの順位。同着は番号を共有）であって `row.rank`（内部の連番）ではない
（[StandingsTable.tsx:93,181](../src/components/StandingsTable.tsx)）。

つまり**変更前のコードは既に自己整合していた** — Ipswich は「13」の隣に「▼1」で 12位→13位。
「14位の隣に▼1が出るのは矛盾」という当初の指摘そのものが、rank を表示だと思い込んだ誤読だった。
変動だけを連番基準に変えた結果、同着2番目以降の3クラブ（Newcastle・Ipswich・Tottenham）で
バッジとツールチップが食い違う**新規の不具合を作り込んだ**。

**対応**: 変更を全面的に差し戻し、`getTableAtMatchday` を共有順位（competition ranking）に戻した。
両端が `position` 基準で揃っていることを不変条件にし、なぜ連番にしてはいけないかをコードに明記。
差し戻し後、40件の矢印すべてでバッジ = 前節順位 − 上昇 + 下降 が成立することをブラウザで実測。

**教訓**: 表示の不整合を直す前に、**その画面が実際に描画している値を確認する。** 型に
`rank` と `position` の両方があるとき、どちらが読者の目に入るかはコードを読まないと分からない。

### 指摘2: 新しい文言が375pxでクラブ名を潰した（R9）

「今季 ベンチ入り3試合・出場なし」（`whitespace-nowrap` で163px）が `flex-1` のクラブ名列を
圧迫し、遠藤航の行で「リヴァプール」が省略記号で切れた。

**対応**: 「今季 ベンチ入り3試合」に短縮した。この行が出る全グループの見出しが既に
「出番なし」「メンバー外」と言っているので、「・出場なし」は重複だった。
375pxで `scrollWidth === clientWidth`（切り詰めなし）、行高49pxを実測。

### 合格していた点

- 再発防止リスト R1（375px横スクロール）は `/` `/standings` とも再発なし
- オリジナリティ 8/10。モノクロ制約の範囲内

### 差分レビュー（2回目）: **合格**

デザイン 7 / オリジナリティ 8 / **クラフト 8** / **機能性 8** — 閾値割れの2項目が回復。

- 指摘1: 解消。Newcastle・Liverpool・Ipswich・Crystal Palace・Aston Villa・Tottenham の
  バッジと `title` を全件突き合わせて矛盾ゼロ。ホームの要約文とも整合
- 指摘2: 解消。375pxで `.truncate` 要素の overflow 0件
- 他を壊していないこと（`/` `/standings` の横スクロール、`.truncate` 全件）も確認

### 持ち越し

1. **`position`（同着共有）と `rank`（連番）の二重構造が温存されている。** 今回は自己整合を
   確認できたが、将来また取り違えるリスクは構造的に残る。`sharedPosition` / `internalRank` の
   ように命名で区別するか、型レベルで分ける価値がある（qa-reviewer も2回続けて指摘）
2. Turbopack の Fast Refresh 直後に `ReferenceError: teamsInTableOrder is not defined` が
   一度だけ観測された（以降十数回の再読み込みで再発なし、ビルドは通過）。根本原因は未特定。
   dev サーバーを完全に再起動した直後の初回ロードで再現するか、次回確認する

---

## 2026-09-09 — 検出した4件の修正

**対象**: [src/lib/data.ts](../src/lib/data.ts) の出場時間導出・名前照合・選手名簿
**判定**: 不変条件 **130件中 不合格0 / 注意11**。`npm run lint` / `npm run build` も通過

### 直したもの

**① 交代イベントが「ピッチにいない選手」を退場させていた（R4）**

`computeMatchMinutes` と `computeSquadMinutesByLineupId` を、**ピッチ上の11人を追跡する**方式に
書き換えた。出場時間は「入った時刻から出た時刻まで」で測る。以前は交代イベントの分数を
そのまま代入していたため、2つの誤りが同時に起きていた。

- ベンチの選手にOUTイベントが付くと、出ていない試合に出場時間が生まれる
  （第3節 Man City の Enzo Fernández に76分。修正後は0分）
- 途中出場した選手が後で交代すると、投入時刻を無視して時計の数字がそのまま入る
  （60分投入・80分交代なら80分と記録されていた。正しくは20分）

**② 名前照合が略称と翻字の重複を扱えなかった（R4）**

`partwiseMatch`（語ごとの照合。"S. W. Egeli" ↔ "Sindre Walle Egeli"、
"Y. Yarmolyuk" ↔ "Yehor Yarmoliuk"）、`prefixMatch`（後ろの語が落ちた形。
"Alysson Edward" ↔ "Alysson Edward Franco da Rocha"）、`initialAndSurnameMatch`
（"I. Fatawu" ↔ "Abdul Fatawu Issahaku"）の3規則を追加。

**③ 照合が「最初の一致」を返していた（R6 — 修正の途中で自ら作り込んだ）**

②の規則を足した直後、Coventry City で「B. Thomas-Asante」が同じクラブの
「Bobby Thomas」に一致し、Thomas-Asante の交代が別人に付いた。緩い規則を足すなら、
**候補は最も強く一致したものを選ばなければならない。** `nameMatchScore` で規則に強さの
順位（完全一致5 → 構造的一致4 → 翻字3 → 単名2 → 頭文字+姓1）を与え、
`bestNameMatch` が最高得点の候補を返すようにした。

**④ 同一選手が提供元の別IDで二重計上（R5）**

`getSquadUsage` の集計キーを、名簿の選手に解決できるときは名簿IDにした。
Liverpool の Jérémy Jacquet は 160分（ローテーション）と 77分（出番待ち）の2行に
分裂していたが、正しい **237分・不動** の1行になった。

**⑤ ありえない年齢を表示していた**

`getPlayers()` が 15〜45歳の範囲外を `null` にする。表示は既存の `-` フォールバックに乗る。
生年月日の誤り自体は提供元由来で直せないので、件数は「注意」として監査が報告し続ける。

**⑥ null と 0 の区別が失われていた（R7）**

`getJapanesePlayerSummaries` の `minutes` は、ラインナップに名前があって出場0なら `0`、
一度も名前がなければ `null` を返す。以前は両方 `null` で、**3試合ベンチ入りしている遠藤航が、
一度も招集されていない選手と同じ扱い**になっていた。`src/lib/types.ts` に書かれていた契約に
実装が追いついていなかった形。現状のUIは両者を同じ文言で表示するため見た目は変わらないが、
「ベンチ入り3試合・出場なし」と書き分ける余地ができた（S09の持ち越し候補）。

### 直していないもの（提供元の欠損。監査は「注意」で報告し続ける）

- **片側しか記録されていない交代 4件**（Man City +14分 / Chelsea +8分 / Sunderland +7分 /
  Newcastle ±0分）。投入された選手は実際にプレーしているので出場時間は付ける。誰が退いたかは
  データにないので、退場者を捏造せず、ずれを `SquadUsage.imbalance` として**導出側が申告する**。
  監査はこの恒等式（実測 = 理論値 + 申告値）を検証するので、申告と実測が1分でも食い違えば
  不合格になる
- **得点イベントとスコアの不一致 6/30試合** — 導出したゴール・アシストが実際より少ない
- **生年月日の誤り 2人** — Lewis Hall、El Hadji Malick Diouf

### data-auditor の判定: **合格**（10ターン）

日本人選手9人の第3節を、ラインナップと交代イベントから手計算して突き合わせ、9人とも一致。
Jacquet の統合（237分・1行）、年齢2人の非表示、`bestNameMatch` が
"B. Thomas-Asante" を Bobby Thomas より優先すること、`getPlayerRoundUsage` と
`getPlayerAppearances` の経路一致も確認。新規の不合格なし。

マネージャー側で9人を独立に再確認済み（冨安健洋が控えで投入なし＝`benched`、
遠藤航の通算が `0`、三笘薫が `null` であることを含む）。
なお data-auditor の報告はクラブ名を2か所取り違えていた（坂元達裕と Bobby Thomas を
Nottm Forest としていたが、実際はどちらも Coventry City）。判定の中身には影響しないが、
**このエージェントの報告に出るクラブ名は鵜呑みにしないこと。**

### 判断が要る持ち越し

1. **表示順位と変動の基準のずれ 3クラブ** — 同着があると、表示は `rank`（1〜20の連番）、
   変動は `position`（同着は共有）で計算される。Ipswich は「14位」の隣に「▼1」が出るが
   基準は 12位→13位。意図的な設計か、揃えるべきかの判断が要る
2. **ベンチ入りの表示** — ⑥で `0` と `null` を分けたが、UIはまだ両方「今季の出場記録なし」。
   「ベンチ入り3試合・出場なし」と書き分けるかどうか
3. **愛称由来の略称が照合できない** — Newcastle の "T. Livramento"（Tino）は正式名
   "Valentino Livramento" と頭文字が食い違い、全規則で0点になる。今回は交代が90分で
   影響0分だったが、同型の選手が試合を左右する場面では無視され続ける。
   `nameMatchScore` の規則ではなく、愛称の対応表で解くべき問題の可能性がある
4. **得点イベントが欠けている6試合** — 交代イベントにも欠損が紛れていないか、
   この6試合に限定して出場時間の手計算を当てる価値がある

---

## 2026-09-09 — 検査体制の導入時ベースライン

**対象**: 既存実装全体（S01〜S10, S13 の累積）
**実施**: `data-auditor` 相当の検算をマネージャーが直接実行（エージェント新設と同時のため）
**判定**: **不合格**（実バグ3件）

### 不変条件
126件中 **不合格3件 / 注意9件**。

### 不合格の詳細

**① 交代イベントが「ピッチにいない選手」を退場させ、出場時間が壊れる**

7クラブで「クラブの総出場時間 = 被覆試合数 × 11人 × 90分」が成立しない。
Man City +90分 / Brentford −89分 / Ipswich +32分 / Fulham +28分 / Chelsea +8分 /
Sunderland +7分 / Aston Villa −15分。

原因は3つに分かれる。いずれも [src/lib/data.ts](../src/lib/data.ts) の
`computeMatchMinutes` と `computeSquadMinutesByLineupId` が
**「OUTになった選手は必ずピッチにいた」と仮定している**ことに帰着する。

- **控えの選手にOUTイベントが付く** — 第3節 Man City、控えの Enzo Fernández に
  76分のOUTが記録されている。彼は投入されていないのに76分が加算される（+90分の主因）
- **交代相手が `null`** — Chelsea 第2節82分、Sunderland 第2節83分。OUT側が空のまま
  IN側だけ加算され、総和が増える
- **略称と翻字の揺れが重なると照合できない** — 控えリスト "Yehor Yarmoliuk" に対し
  イベントは "Y. Yarmolyuk"。略称ルールは翻字違いを吸収できず、翻字ルールは
  文字数差4で発動しない（`transliterationMatch` の閾値は2）。Brentford −89分の主因

**読者への影響**: 選手ページの「出場時間」「◯人中◯番手」「不動／ローテーション／出番待ち」が
実際と食い違う。S09 の中心命題「その選手はレギュラーなのか」に直接効く。

**② 同一選手が提供元の別IDで二重に数えられる**

Liverpool の Jérémy Jacquet が、提供元の2つのID（59189550 "Jérémy Jacquet" /
84998977 "J. Jacquet"）で別人として集計されている。160分（ローテーション）と
77分（出番待ち）の2行に分裂し、実際の237分がどこにも出ない。
`getSquadUsage` が提供元IDをキーにしているため（[src/lib/data.ts](../src/lib/data.ts)）。

**読者への影響**: 該当選手の出場時間が過小、序列と役割が誤り。

**③ 生年月日が明らかに誤っている選手が2人いる**

Lewis Hall (`2025-06-30` → 1歳)、El Hadji Malick Diouf (`2023-02-21` → 3歳)。
football-data.org 由来の誤りで、こちらでは直せない。ただし**そのまま表示している**のが問題。

**読者への影響**: 選手ページに「1歳」と出る。データ全体の信用を落とす。

### 注意（失敗ではないが記録する）

- **得点イベントとスコアの不一致 6/30試合** — 提供元のイベント欠損。導出したゴール・
  アシストが実際より少なくなる。ランキングの母数に影響する
- **表示順位と変動の基準のずれ 3クラブ** — 同着があると、表示は `rank`（1〜20の連番）、
  変動は `position`（同着は共有）で計算される。Ipswich は「14位」の隣に「▼1」が出るが、
  基準は 12位→13位。設計判断として意図的かどうかの確認が要る
- **名簿と照合できた出場時間 99.0%** — 残り1%は players.json に載っていない選手
- **未取得ペア 0 / 190** — H2H は全ペア取得済み

### 次のイテレーションで狙うべき検算

1. 日本人選手9人の直近節の出場時間を、ラインナップから手計算して突き合わせる（今回未実施）
2. `benched` と `absent` の判定精度 — 控えリストに名前があるのに `absent` と分類されて
   いる選手がいないか。製品仕様が「未解決の確認事項」として挙げたまま残っている
