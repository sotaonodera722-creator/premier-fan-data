# S◯◯ — （スプリント名）

> 1行で。このスプリントが終わったとき、読者にとって何ができるようになるか。

- **対象読者**: 層A / 層B / 層C（[product-spec.md](../product-spec.md) の4層）
- **状態**: 設計中 / 承認済み / 実装中 / 検査中 / 完了
- **承認**: （代表の承認日。未承認なら空欄。**空欄のまま実装に入らない**）

---

## 1. 含む

実装する機能を、**読者から見た振る舞い**で書く。「`getFoo()` を追加する」ではなく
「選手ページで、その選手が次にいつ出場するかが分かる」。

- [ ]
- [ ]

## 2. 含まない

**このスプリントで作らないもの。** ここが契約の本体である。
検査者はここに書かれた不足を減点してはならない。

- （機能名） — 理由（どのスプリントに送るか、なぜ今やらないか）

## 3. 完了条件

「できた」と言える条件を、**検証できる形で**書く。各項目に検証方法を付ける。
書けない条件は完了条件ではないので、書き直すか捨てる。

| # | 条件 | 誰が検証するか | 検証方法 |
|---|---|---|---|
| 1 | | qa-reviewer / data-auditor | |

数字に関わる条件は必ず `data-auditor` に、見た目と操作に関わる条件は `qa-reviewer` に
割り当てる。両方に割り当てる条件があってもよい。

## 4. データの前提

この機能が依存するデータと、**それが欠けているときの表示**。
[product-spec.md](../product-spec.md) の「実データ検証で判明した訂正」を必ず確認すること。

| 依存するデータ | 欠損の可能性 | 欠けているときの表示 |
|---|---|---|

## 5. 設計上の判断

後から蒸し返さないために、迷った点と決めた理由を残す。

-

## 6. 制約の確認

- [ ] モノクロ方針を破っていない（色はクラブのアイデンティティにのみ使う）
- [ ] `--success` / `--info` / `--danger` を装飾に使っていない
- [ ] モバイル375pxで横スクロールしない
- [ ] データが空のときの表示が定義されている
- [ ] [qa-log.md](../qa-log.md) の再発防止リストに抵触しない

---

## 検査結果

（実装後に `qa-reviewer` と `data-auditor` の判定を要約して貼る。
詳細は [qa-log.md](../qa-log.md) に残す）
